<h1>Products</h1>
<% @products.each do |product| %>
  <div>
    <h2><%= product.name %></h2>
    <p><%= product.description %></p>
    <p><%= number_to_currency(product.price) %></p>
    <%= button_to 'Add to Cart', add_to_cart_cart_path(product_id: product.id), method: :post %>
  </div>
<% end %>